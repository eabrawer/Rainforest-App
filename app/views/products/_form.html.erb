<%= form_for @product do |form| %>
	<%= form.label :name %><br>
	<%= form.text_field :name %><br>

	<%= form.label :description %><br>
	<%= form.text_area :description %><br>

	<%= form.label :price_in_cents %><br>
	<%= form.number_field :price_in_cents %><br>

	<%= form.submit "Save" %>
<% end %>
